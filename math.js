const sum = (barca, madrid) => barca + madrid;
const sub = (barca, madrid) => barca - madrid;

const calculator = {
  multiply: (p, q) => {
    return p * q;
  },
};

module.exports = { sum, calculator, sub };
